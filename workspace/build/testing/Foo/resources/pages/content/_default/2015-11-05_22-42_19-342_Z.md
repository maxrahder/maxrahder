Some documentation uses the term *scope* for the idea of the invocation object. 
But *scope* can also refer to the lexical, compile-time
variable and function scope.
So if you use the term *scope*, be careful to clarify which concept you mean.