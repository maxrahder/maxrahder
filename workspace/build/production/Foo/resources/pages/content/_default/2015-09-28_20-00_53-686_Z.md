The microloader code is in `bootstrap.js`. 

It uses the information  in `bootstrap.json` to determine what resources to load. 

As a bonus, when you build your application, the microloader uses application caching 
to make your application load as quickly as possible.