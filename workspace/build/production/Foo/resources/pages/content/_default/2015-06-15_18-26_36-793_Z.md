Using +config+ to define properties will result in the class system automatically generating accessor methods.

The class system will also generate an _apply_ and _update_ method for each config property.